Description:
  Creates an HTML test file for a JavaScript library.

Usage: 
  #{$0} #{spec.name} name [library_name]"

Notes:
* name - creates a file test/name_test.html
* library_name - is for a file src/library_name.js
